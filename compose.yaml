services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: geospatial-api:local
    container_name: geospatial-api
    env_file: .env
    environment:
      NODE_ENV: development
    ports:
      - '3000:3000'
    restart: unless-stopped
